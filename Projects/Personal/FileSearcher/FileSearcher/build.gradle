ext {
    mainClass = 'com.personal.scripts.file_search.AppStart' + project.name
    projectVersion = '1.0.4'
    jarFolderPath = 'C:\\IVI\\Scripts\\FileSearcher'

    subProjectNames = [
            ':app-info',
            ':gui-utils'
    ]
    compileClassPaths = [
            'org.apache.tika:tika-parser-text-module:3.2.3'
    ]
    compileJarFilePaths = []
    testCompileClassPaths = []
    testCompileJarFilePaths = []
}

apply from: gradle.ext.rootDirPath + '/common_build.gradle'
